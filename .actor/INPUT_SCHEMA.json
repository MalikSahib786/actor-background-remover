{
    "title": "Pro AI Background Remover",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "imageUrl": {
            "title": "Image URL",
            "type": "string",
            "description": "Direct URL to the image to process.",
            "editor": "textfield"
        },
        "imageBase64": {
            "title": "Image Base64",
            "type": "string",
            "description": "Alternative: Base64 encoded string of the image.",
            "editor": "textarea"
        },
        "enableAlphaMatting": {
            "title": "Enable Alpha Matting (High Precision)",
            "type": "boolean",
            "description": "Highly recommended for hair, fur, or semi-transparent objects. Takes slightly longer but produces much better edges.",
            "default": true
        },
        "outputFormat": {
            "title": "Output Format",
            "type": "string",
            "description": "Select the file format for the processed image (png or webp).",
            "default": "png",
            "enum": ["png", "webp"],
            "editor": "select"
        }
    },
    "required": []
}
